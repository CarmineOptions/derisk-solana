import pandas



# TODO: To be implemented.
def get_events(protocol: str, start_block_number: int = 0) -> pandas.DataFrame:
    # TODO: Fetch transactions from the database and transform them to events.
    return pandas.DataFrame(columns = ['block_number', 'timestamp'])